{
  gtk = {
    enable = true;
    catppuccin = {
      enable = true;
      size = "compact";
      accent = "peach";
      cursor = {
        enable = true;
        accent = "dark";
      };
    };
  };
}
